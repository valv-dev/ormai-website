---
import Layout from "../layouts/Layout.astro";
import {ParticleField} from "../components/ParticleField"
import {ParticleBackground} from "../components/ParticleBackground"
import {ParticleDivider} from "../components/ParticleDivider"
import {Faq} from "../components/Faq"
import {Reveal} from "../components/Reveal"
import {InstallTabs} from "../components/InstallTabs"
import WhyGrid from "../components/WhyGrid.astro"
import {codeToHtml} from "shiki"
import {TS_CODE} from "../lib/snippets"

const theme = "vesper"
const highlighted = {
	prisma: await codeToHtml(TS_CODE.prisma, {lang: "typescript", theme}),
	clickhouse: await codeToHtml(TS_CODE.clickhouse, {lang: "typescript", theme}),
}
---

<Layout>
	<div class="relative isolate w-full">
		<ParticleBackground client:load />
		<ParticleBackground client:load flipped className="h-[25lvh]" />
		<div class="w-full flex flex-col gap-8 max-w-3xl mx-auto px-4 pt-24 md:pt-32 pb-16 selection:bg-blue-800/20">
			<ParticleField client:load />
			<h1 class="text-center text-neutral-200 text-4xl md:text-5xl text-balanced font-medium -mt-4 leading-9 md:leading-14 tracking-tight motion-preset-blur-up-lg motion-delay-300">
				Let agents query your database. Just not all of it.
			</h1>
			<div class="flex flex-col items-center md:flex-row gap-4 mx-auto mt-4 motion-preset-blur-up-lg motion-delay-500">
				<a href="/docs/" class="h-12 w-52 flex items-center justify-center md:w-40 rounded-sm bg-fg text-bg text-sm font-medium cursor-pointer hover:-translate-y-0.5 transition-all">
					Quick Start
				</a>
				<a href="https://github.com/valv-dev/valv" target="_blank" rel="noreferrer" class="h-12 w-40 flex flex-row items-center gap-0.5 justify-center rounded-sm bg-bg ring-[0.5px] ring-fg-faint text-sm text-fg-muted cursor-pointer hover:text-fg hover:ring-fg-muted transition-all hover:-translate-y-0.5 group">
					<i class="ph ph-star pr-1 inline-block"></i>
					Star on GitHub
				</a>
			</div>
			<div class="w-full flex flex-col gap-32 mb-40 motion-preset-blur-up-lg motion-translate-y-in-[3%] motion-delay-600">
				<div class="w-full mt-32">
						<InstallTabs highlighted={highlighted} client:visible />
				</div>
				<ParticleDivider className="-mt-20 -mb-20" client:load />
				<Reveal client:visible className="w-full flex flex-col gap-4">
					<div class="relative left-1/2 w-[92vw] max-w-5xl -translate-x-1/2 flex flex-col gap-10">
						<h2 class="text-2xl font-medium text-center">Why use it?</h2>
						<WhyGrid />
					</div>
				</Reveal>
				<Reveal client:visible className="w-full flex flex-col gap-4">
					<h2 class="text-2xl font-medium text-center">Benchmarks</h2>
					<div class="w-full h-[300px] rounded-sm ring-[1px] ring-border bg-bg flex items-center justify-center">
						<span class="text-xs font-medium opacity-30">benchmarks</span>
					</div>
				</Reveal>
				<Reveal client:visible className="w-full flex flex-col gap-4">
					<h2 class="text-2xl font-medium text-center">FAQ</h2>
					<Faq client:visible />
				</Reveal>
			</div>
		</div>
		<footer class="relative w-full max-w-3xl mx-auto px-4 pb-16 flex items-center justify-center gap-6 text-sm text-fg-muted">
			<a href="https://github.com/valv-dev/valv" target="_blank" rel="noreferrer" class="flex items-center gap-1.5 hover:text-fg transition-colors">
				<i class="ph ph-github-logo"></i>
			</a>
			<a href="/docs/" class="hover:text-fg transition-colors">
				Docs
			</a>
		</footer>
	</div>
</Layout>
